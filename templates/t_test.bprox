[ABrox Project File]
{
    "Analysis": {
        "data": {
            "datafile": null
        },
        "models": [
            {
                "simulate": "def simulate(params):\n    n = 50\n    first_sample = np.random.normal(0,1,n)\n    sec_sample = np.random.normal(params['d\u2018],1,n)\n    return np.column_stack((first_sample,sec_sample))",
                "name": "Model1",
                "priors": [
                    {
                        "d": "stats.cauchy(loc=0.0, scale=0.7)"
                    }
                ]
            },
            {
                "simulate": "def simulate(params):\n    n = 50\n    first_sample = np.random.normal(0,1,n)\n    sec_sample = np.random.normal(0,1,n)\n    return np.column_stack((first_sample,sec_sample))",
                "name": "Model2",
                "priors": []
            }
        ],
        "summary": "def summary(params):\n    diff_mean = np.subtract(np.mean(data,axis=0))\n    mean_std = np.mean(np.std(data,axis=0)\n    return diff_mean / mean_std\n    ",
        "distance": "def distance(params):\n    # write your code here\n    pass",
        "settings": {
            "objective": "comparison",
            "threshold": -1,
            "modeltest": false,
            "distance_metric": "default",
            "fixedparameters": [],
            "percentile": 0.05,
            "method": "logistic",
            "outputdir": "",
            "particles": 1000.0
        }
    }
}